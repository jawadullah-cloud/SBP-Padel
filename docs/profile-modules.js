(function(){
  'use strict';
  if(window.__SBPProfileModules)return;
  window.__SBPProfileModules=true;
  const css=document.createElement('link');css.rel='stylesheet';css.href='profile-modules.css?v=20260825-favourites-live2';document.head.appendChild(css);
  const app=document.querySelector('.app'),nav=document.querySelector('nav'),profile=document.getElementById('profile');if(!app||!nav||!profile)return;
  const API=(localStorage.getItem('sbpPadelApiBase')||'http://127.0.0.1:8000/api/v1').replace(/\/$/,'');
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const initials=n=>String(n||'').split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]?.toUpperCase()).join('');
  const playersKey='sbpPadelSavedPlayers',favKey='sbpPadelFavouriteVenueIds';
  function makeScreen(id){let s=document.getElementById(id);if(s)return s;s=document.createElement('section');s.className='screen';s.id=id;app.insertBefore(s,nav);return s}
  const saved=makeScreen('savedPlayers'),favs=makeScreen('favouriteVenues'),help=makeScreen('helpSupport');
  function switchScreen(id){document.querySelectorAll('.screen').forEach(s=>s.classList.toggle('active',s.id===id));document.querySelectorAll('.nav').forEach(n=>n.classList.toggle('active',n.dataset.nav==='profile'));nav.classList.remove('flowHidden');document.getElementById(id)?.scrollTo({top:0,behavior:'auto'})}
  const backProfile=()=>switchScreen('profile');
  function getPlayers(){try{const rows=JSON.parse(localStorage.getItem(playersKey)||'[]');return Array.isArray(rows)?rows:[]}catch{return[]}}
  function setPlayers(rows){localStorage.setItem(playersKey,JSON.stringify(rows))}
  function getFavIds(){try{const rows=JSON.parse(localStorage.getItem(favKey)||'[]');return Array.isArray(rows)?rows.map(String):[]}catch{return[]}}
  function setFavIds(rows){const ids=[...new Set(rows.map(String))];localStorage.setItem(favKey,JSON.stringify(ids));window.dispatchEvent(new CustomEvent('sbp-favourites-change',{detail:{ids}}));return ids}
  async function getVenues(){try{const r=await fetch(`${API}/venues?_=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error();const rows=await r.json();return Array.isArray(rows)?rows:[]}catch{return[]}}
  function directionsFor(v){if(v?.latitude==null||v?.longitude==null)return'';return `https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(v.latitude)},${encodeURIComponent(v.longitude)}`}
  function featureText(v){const count=Array.isArray(v?.courts)?v.courts.length:null;const bits=[];if(count)bits.push(`${count} Court${count===1?'':'s'}`);if(Array.isArray(v?.amenities)&&v.amenities.length)bits.push(...v.amenities.slice(0,2));return bits.join(' · ')||'SBP Padel facility'}
  function openExternal(url){if(!url)return;if(typeof window.SBPPlayerOpenExternal==='function'){window.SBPPlayerOpenExternal(url);return}try{if(window.SBPAndroid&&typeof window.SBPAndroid.openExternal==='function'){window.SBPAndroid.openExternal(url);return}}catch{}location.href=url}
  function renderPlayers(){const rows=getPlayers();saved.innerHTML=`<div class="pmWrap"><div class="pmHead"><button type="button" class="pmBack" data-pm-back>←</button><div><small>PROFILE</small><h1>Saved Players</h1></div></div><p class="pmIntro">Keep regular playing partners ready so they can be added to a court booking quickly.</p><section class="pmAdd"><label>ADD NEW PLAYER</label><div class="pmAddRow"><input id="pmPlayerName" maxlength="40" placeholder="Player name"><button type="button" id="pmAddPlayer">ADD</button></div></section><div class="pmSectionTitle"><h2>Your players</h2><small>${rows.length} saved</small></div><div class="pmList">${rows.map((p,i)=>`<article class="pmPlayer"><div class="pmAvatar">${esc(initials(p))}</div><div><b>${esc(p)}</b><small>Saved playing partner</small></div><button type="button" class="pmRemove" data-remove-player="${i}">×</button></article>`).join('')}</div><div class="pmEmpty ${rows.length?'':'show'}">No saved players yet.<br>Add a regular playing partner above.</div></div>`}
  function addPlayer(){const input=saved.querySelector('#pmPlayerName'),name=(input?.value||'').trim();if(!name)return;const rows=getPlayers();if(!rows.some(p=>p.toLowerCase()===name.toLowerCase()))rows.push(name);setPlayers(rows);renderPlayers()}
  async function renderFavs(){
    const ids=getFavIds();
    favs.innerHTML=`<div class="pmWrap"><div class="pmHead"><button type="button" class="pmBack" data-pm-back>←</button><div><small>PROFILE</small><h1>Favourite Venues</h1></div></div><p class="pmIntro">Your saved SBP Padel facilities appear here for faster access and booking.</p><div class="pmSectionTitle"><h2>Saved venues</h2><small>${ids.length?`${ids.length} venue${ids.length===1?'':'s'}`:'None yet'}</small></div><div class="pmEmpty ${ids.length?'':'show'}">${ids.length?'Loading saved venues…':'No favourite venues yet.<br>Save a venue to see it here.'}</div></div>`;
    if(!ids.length){window.SBPRefreshBackIcons?.();return}
    const venues=await getVenues();
    const rows=ids.map(id=>venues.find(v=>String(v.id)===id)).filter(Boolean);
    const wrap=favs.querySelector('.pmWrap');if(!wrap)return;
    const empty=wrap.querySelector('.pmEmpty');
    if(!rows.length){if(empty){empty.classList.add('show');empty.innerHTML='Saved venues could not be loaded.<br>Try again when the venue service is available.'}window.SBPRefreshBackIcons?.();return}
    empty?.remove();
    wrap.insertAdjacentHTML('beforeend',rows.map(v=>{
      const directions=directionsFor(v);
      return `<article class="pmVenue" data-fav-venue="${esc(v.id)}"><div class="pmVenueImage"><span class="pmVenueBadge">OPEN · ${esc(String(v.city||'Punjab').toUpperCase())}</span><button type="button" class="pmHeart" data-remove-favourite="${esc(v.id)}" aria-label="Remove ${esc(v.name)} from favourites">♥</button></div><div class="pmVenueBody"><h3>${esc(v.name)}</h3><p>${esc(featureText(v))}</p><div class="pmVenueMeta">${(v.amenities||[]).slice(0,3).map(a=>`<span>${esc(a)}</span>`).join('')}</div><div class="pmVenueActions"><button type="button" class="pmExplore" data-open-favourite="${esc(v.id)}">EXPLORE VENUE</button>${directions?`<button type="button" class="pmDirections" data-fav-directions-url="${esc(directions)}">DIRECTIONS ↗</button>`:''}</div></div></article>`
    }).join(''));
    window.SBPRefreshBackIcons?.();
  }
  function removeFavourite(id){setFavIds(getFavIds().filter(x=>x!==String(id)));if(favs.classList.contains('active'))renderFavs();window.SBPPlayerVenuesRefresh?.()}
  function renderHelp(){help.innerHTML=`<div class="pmWrap"><div class="pmHead"><button type="button" class="pmBack" data-pm-back>←</button><div><small>SUPPORT</small><h1>Help & Support</h1></div></div><p class="pmIntro">Quick answers and support for bookings, payments and SBP Padel facilities.</p><section class="pmSupportHero"><div class="pmSupportIcon">?</div><div><small>SBP PADEL SUPPORT</small><h2>How can we help?</h2><p>Find a quick answer below.</p></div></section><div class="pmSectionTitle"><h2>Frequently asked</h2></div><div class="pmFaq"><button type="button" data-faq><span><b>How do I cancel a booking?</b><small>Cancellation and refund process</small></span><i>＋</i></button><div class="pmFaqAnswer">Open My Bookings, select the upcoming booking and choose Cancel Booking. Any eligible refund will be shown before confirmation.</div><button type="button" data-faq><span><b>Can I reschedule my court?</b><small>Changing date or time</small></span><i>＋</i></button><div class="pmFaqAnswer">Open booking details and choose Reschedule. The replacement slot must currently match the already-paid court fee.</div><button type="button" data-faq><span><b>What if the venue closes?</b><small>Venue-side closure</small></span><i>＋</i></button><div class="pmFaqAnswer">Venue-side changes appear in booking activity and any applicable refund or rescheduling process.</div></div></div>`;window.SBPRefreshBackIcons?.()}
  profile.addEventListener('click',e=>{const btn=e.target.closest('.menu button');if(!btn)return;const label=btn.querySelector('span')?.textContent?.trim();if(['Notifications','Appearance','My Wallet','Payment History','My Bookings','Sign Out'].includes(label))return;if(label==='Saved Players'){e.preventDefault();renderPlayers();switchScreen('savedPlayers')}if(label==='Favourite Venues'){e.preventDefault();switchScreen('favouriteVenues');renderFavs()}if(label==='Help & Support'){e.preventDefault();renderHelp();switchScreen('helpSupport')}});
  saved.addEventListener('click',e=>{if(e.target.closest('[data-pm-back]')){backProfile();return}if(e.target.closest('#pmAddPlayer')){addPlayer();return}const rm=e.target.closest('[data-remove-player]');if(rm){const rows=getPlayers();rows.splice(Number(rm.dataset.removePlayer),1);setPlayers(rows);renderPlayers()}});
  saved.addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target.id==='pmPlayerName'){e.preventDefault();addPlayer()}});
  favs.addEventListener('click',e=>{if(e.target.closest('[data-pm-back]')){backProfile();return}const rm=e.target.closest('[data-remove-favourite]');if(rm){e.preventDefault();removeFavourite(rm.dataset.removeFavourite);return}const open=e.target.closest('[data-open-favourite]');if(open){e.preventDefault();window.SBPPlayerOpenVenue?.(open.dataset.openFavourite);return}const dir=e.target.closest('[data-fav-directions-url]');if(dir){e.preventDefault();e.stopPropagation();openExternal(dir.dataset.favDirectionsUrl);return}});
  help.addEventListener('click',e=>{if(e.target.closest('[data-pm-back]')){backProfile();return}const faq=e.target.closest('[data-faq]');if(!faq)return;const answer=faq.nextElementSibling;if(!answer)return;answer.classList.toggle('open');const i=faq.querySelector('i');if(i)i.textContent=answer.classList.contains('open')?'−':'＋'});
  window.addEventListener('sbp-favourites-change',()=>{if(favs.classList.contains('active'))renderFavs()});
})();
