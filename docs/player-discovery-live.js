(()=>{'use strict';if(window.__SBPDiscoveryLive)return;window.__SBPDiscoveryLive=true;const STATE='sbpPadelBookingSessionV2';const apiBase=()=>window.SBPApiBase?.()||(localStorage.getItem('sbpPadelApiBase')||'http://127.0.0.1:8000/api/v1').replace(/\/$/,'');let venues=[],mode='all',city='';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const rad=x=>x*Math.PI/180;const km=(a,b)=>{const R=6371,dLat=rad(Number(b.latitude)-a.lat),dLon=rad(Number(b.longitude)-a.lon),x=Math.sin(dLat/2)**2+Math.cos(rad(a.lat))*Math.cos(rad(Number(b.latitude)))*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(x))};const fmt=t=>{const[h,m]=String(t).split(':').map(Number);return new Date(2000,0,1,h,m||0).toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'})};async function json(path){const r=await fetch(`${apiBase()}${path}`,{cache:'no-store'});if(!r.ok)throw new Error(String(r.status));return r.json()}function getPos(){return new Promise((resolve,reject)=>{if(!navigator.geolocation)return reject(new Error('Location unavailable'));navigator.geolocation.getCurrentPosition(p=>resolve({lat:p.coords.latitude,lon:p.coords.longitude}),reject,{enableHighAccuracy:false,timeout:6500,maximumAge:300000})})}function filters(){const host=document.querySelector('#venues .filters');if(!host)return;const cities=[...new Set(venues.map(v=>String(v.city||'').trim()).filter(Boolean))].sort();host.innerHTML=`<button data-disc="all" class="${mode==='all'?'selected':''}">All</button><button data-disc="near" class="${mode==='near'?'selected':''}">Near Me</button>${cities.map(c=>`<button data-disc-city="${esc(c)}" class="${mode==='city'&&city===c?'selected':''}">${esc(c)}</button>`).join('')}`;host.style.overflowX='auto';host.style.justifyContent='flex-start';host.querySelectorAll('button').forEach(b=>b.style.flex='0 0 auto')}function cards(rows){const root=document.querySelector('#venues .content.tight');if(!root)return;root.innerHTML=rows.length?rows.map(v=>`<article class="venueLarge sbpVenueCard" data-sbp-venue="${esc(v.id)}"><div class="venueLargeImage courtScene"><span class="badge green">OPEN</span><span class="count">${esc(v.distanceText||v.city)}</span></div><div class="venueLargeCopy"><p class="overline">${esc(String(v.city||'Punjab').toUpperCase())}</p><h3>${esc(v.name)}</h3><p>${esc(v.address||'Sports Board Punjab Padel facility')}</p><div class="amenityLine">${(v.amenities||[]).slice(0,3).map(a=>`<span>${esc(a)}</span>`).join('')}</div><button class="secondary" type="button">Explore venue <b>→</b></button></div></article>`).join(''):'<div class="coming"><div><b>No matching venues</b><p>Try another facility filter.</p></div></div>';window.SBPVenueCover?.scan?.()}async function apply(modeNext,cityNext=''){mode=modeNext;city=cityNext;filters();if(mode==='city')return cards(venues.filter(v=>String(v.city)===city));if(mode!=='near')return cards(venues);const host=document.querySelector('#venues .content.tight');if(host)host.innerHTML='<div class="coming"><div><b>Finding nearby courts…</b><p>Allow location access to sort facilities by distance.</p></div></div>';try{const p=await getPos(),rows=venues.map(v=>({...v,distance:km(p,v)})).sort((a,b)=>a.distance-b.distance).map(v=>({...v,distanceText:`${v.distance.toFixed(v.distance<10?1:0)} km`}));cards(rows)}catch{mode='all';filters();cards(venues);window.dispatchEvent(new CustomEvent('sbp-toast',{detail:'Location unavailable. Showing all venues.'}))}}async function nextAvailability(){const card=document.querySelector('#home .nextRich');if(!card||!venues.length)return;const today=new Date();today.setHours(0,0,0,0);let p=null;try{p=await getPos()}catch{}let ordered=venues.map(v=>({...v,distance:p?km(p,v):null}));if(p)ordered.sort((a,b)=>a.distance-b.distance);card.querySelector('small').textContent='Checking live court availability…';const candidates=[];for(let day=0;day<4;day++){const d=new Date(today);d.setDate(d.getDate()+day);const ds=[d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-');for(const v of ordered){try{const a=await json(`/venues/${v.id}/availability?date=${ds}&_=${Date.now()}`);for(const c of a.courts||[]){const s=(c.slots||[]).find(x=>x.available);if(s){candidates.push({v,c,s,date:ds,day,distance:v.distance});break}}}catch{}}if(candidates.length)break}if(!candidates.length){card.querySelector('small').textContent='No bookable slots in the next 4 days';card.querySelector('h2').textContent='Check courts';card.querySelector('span').textContent='Browse live availability';return}candidates.sort((a,b)=>a.day-b.day||String(a.s.start_time).localeCompare(String(b.s.start_time))||((a.distance??99999)-(b.distance??99999)));const x=candidates[0],label=x.day===0?'Today':x.day===1?'Tomorrow':new Date(`${x.date}T12:00:00`).toLocaleDateString('en-US',{weekday:'short',month:'short',day:'numeric'});card.dataset.liveVenue=x.v.id;card.dataset.liveCourt=x.c.court_id;card.dataset.liveDate=x.date;card.dataset.liveSlot=x.s.start_time;card.querySelector('small').textContent=`${label} · ${x.v.name}${x.distance!=null?` · ${x.distance.toFixed(x.distance<10?1:0)} km`:''}`;card.querySelector('h2').textContent=fmt(x.s.start_time);card.querySelector('span').textContent=`${x.c.court_name} · 60 min`;const img=card.querySelector('.miniCourt');if(img)window.SBPVenueCover?.apply?.(img,x.v.id)}function bookNext(card){const v=venues.find(v=>String(v.id)===String(card.dataset.liveVenue));if(!v)return;let s={};try{s=JSON.parse(localStorage.getItem(STATE)||'{}')}catch{}Object.assign(s,{venueId:v.id,venueName:v.name,courtId:card.dataset.liveCourt,bookingDate:card.dataset.liveDate,slotStarts:[card.dataset.liveSlot],status:'selecting',updatedAt:Date.now()});localStorage.setItem(STATE,JSON.stringify(s));localStorage.setItem('sbpPadelSelectedVenueId',v.id);window.SBPPlayerOpenVenue?.(v.id)}async function load(){try{venues=await json(`/venues?_=${Date.now()}`);filters();cards(venues);venues.forEach(v=>{if(v.cover_image_data_url){const i=new Image();i.src=v.cover_image_data_url}});nextAvailability()}catch(e){console.error('SBP discovery',e)}}document.addEventListener('click',e=>{const b=e.target.closest?.('[data-disc]');if(b){e.preventDefault();apply(b.dataset.disc);return}const c=e.target.closest?.('[data-disc-city]');if(c){e.preventDefault();apply('city',c.dataset.discCity);return}const n=e.target.closest?.('#home .nextRich button');if(n&&n.closest('.nextRich')?.dataset.liveVenue){e.preventDefault();e.stopImmediatePropagation();bookNext(n.closest('.nextRich'))}},true);window.addEventListener('sbp-player-venues-loaded',e=>{if(Array.isArray(e.detail?.venues)){venues=e.detail.venues;filters();cards(venues);nextAvailability()}});setTimeout(load,250)})();